<div class="nested-fields ui segment">
  <div class="field">
    <%= f.text_area :text, placeholder: "Enter your objective here" %>
    <%= link_to_remove_association "remove objective", f, class: "ui bottom attached red fluid button" %>
    <div class="ui divider"></div>
  </div>
  Objectives:
  <div id='tasks' class="ui raised segment">
    <% Line.all.each.with_index do |line,ab| %>

      <%= f.simple_fields_for :lines do |line| %>
      
        <%= render 'lines_fields', :f => line, :index => index %>
      <% end %>
    <% end %>

      <div class='links'>
        <%= link_to_add_association 'add objective', f, :lines, class: "ui button" %>
      </div>
    </div>
</div>
