<%= form_for(@arcticle) do |f| %>
  <% if @arcticle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@arcticle.errors.count, "error") %> prohibited this arcticle from being saved:</h2>

      <ul>
      <% @arcticle.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="field">
    <%= f.label :references %><br>
    <%= f.text_field :references %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
